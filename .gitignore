# Local fork name overrides (user-specific, not committed)
fork-overrides.sh

# Credentials and config
kubeconfig
*.kubeconfig
.env
.env.*
.envrc
*.pem
*.key

# Go local workspace
.local/

# Planning artifacts (workflow config, not codebase docs — those are in reference/)
.planning/

# Claude Code (but keep hooks, settings, rules, workflows)
.claude/*
!.claude/hooks/
!.claude/settings.json
!.claude/rules/
!.claude/workflows/
.mcp.json

# Shared rules vendored via osac-ai-skills (OSAC-4006): symlinks materialized
# by tools/link-agent-skills.sh, not committed. Workspace-only rules (e.g.
# cross-repo-workflow.md) stay real files and remain tracked. (.claude/agents/
# needs no entry here — no !.claude/agents/ exception above, so the blanket
# .claude/* ignore already covers vendored agent symlinks like quick-fix.md.)
.claude/rules/architecture-patterns.md
.claude/rules/networking-design-alignment.md
.claude/rules/request-path-tracing.md
.claude/rules/dev-conventions.md

# Shared hook documentation vendored via osac-ai-skills (OSAC-4006): symlink
# materialized by tools/link-agent-skills.sh, not committed. The actual hook
# scripts (update-ai-context.sh, statusline.sh) are workspace-specific
# executables and stay real, tracked files — only the generic README.md moved.
.claude/hooks/README.md

# Shared design-context content vendored via osac-ai-skills (OSAC-4006):
# symlinks materialized by tools/link-agent-skills.sh, not committed.
# .design/templates/ (hand-synced with enhancement-proposals' design
# template) is unaffected — only these four .design/context/ files moved.
.design/context/enclave-wizard-pipeline.md
.design/context/networking-decisions.md
.design/context/osac-dimensions.md
.design/context/review-patterns.md

# Bootstrap-managed skill overlay (native OSAC skills + ai-workflows
# symlinks). Regenerated by bootstrap.sh / tools/link-agent-skills.sh.
skills/

# Vendored osac-ai-skills (clone/fetch/rebase via bootstrap.sh; OSAC-3956)
.osac-ai-skills/

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# OS files
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.o
*.a

# Generated site index (built by CI and make slides)
/index.html

# Dependencies (language-specific -- uncomment as needed)
# node_modules/
# vendor/
__pycache__/
*.pyc

# Bootstrap-managed repositories (cloned by bootstrap.sh)
osac/
osac-docs/
osac-test-infra/
osac-ui/
enhancement-proposals/
host-management-openstack/
# Temporary: keep ignoring the pre-mono-repo standalone clone paths until
# quarantine has fully soaked (see bootstrap.sh's quarantine_merged_component()).
# A workspace that hasn't re-run bootstrap.sh yet still has these directories
# on disk; without these entries they'd show up as untracked and unprotected
# by Skillsaw. Remove once the old repos are archived upstream (tracked by
# OSAC-1737).
fulfillment-service/
osac-operator/
osac-aap/
osac-installer/
bare-metal-fulfillment-operator/
osac-csi-driver/
cluster-tool/

# Bootstrap-managed reference repos (cloned read-only, no fork remote)
osac-ux/

# Quarantined clones of components merged into osac-project/osac — see
# bootstrap.sh's quarantine_merged_component()
.legacy-repos/

docs/*
!docs/pr-dashboard/
docs/pr-dashboard/data.json
.ai-workflows/

# Workflow artifacts (bugfix, implement)
.artifacts/

# skillsaw baseline not used — strict lint only
.skillsaw-baseline.json
**/.skillsaw-baseline.json

# Workflow artifacts (bugfix, implement) — keep shared config
.artifacts/*
!.artifacts/prd/
.artifacts/prd/*
!.artifacts/prd/config.json

# Containerlab generated state
clab-*/

# AI Tooling
.cursor/
.gemini/
