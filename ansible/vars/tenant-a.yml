---
tenant_name: "tenant-a"
tenant_vlan: 100
tenant_l2_vni: 10100
tenant_l3_vni: 10001
tenant_subnet: "10.100.0.0/24"

# Loopbacks (VTEP IPs — must be unique per leaf, reachable via BGP underlay)
leaf1_loopback: "10.0.0.1/32"
leaf2_loopback: "10.0.0.2/32"
spine_loopback: "10.0.0.3/32"

# BGP AS numbers (each device gets its own ASN for eBGP)
leaf1_asn: 65001
leaf2_asn: 65002
spine_asn: 65000
